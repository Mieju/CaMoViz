// Thin re-export so the rest of the app imports orbit controls from one place
// (matches the module map in CARDIAC_WAVE_VISUALIZER_PLAN.md §3).
export { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';
